import { GetCategoryPipe } from './get-category.pipe';

describe('GetCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
